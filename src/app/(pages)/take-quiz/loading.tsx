import Loader from "@revisela/components/Loader/index";

type Props = {};

const Loading = (props: Props) => {
  return (
    <div className="loader-container">
      <Loader />
    </div>
  );
};

export default Loading;
